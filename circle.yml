general:
  branches:
    only:
      - master

test:
  override:
    - "bundle exec jekyll contentful"
    - "bundle exec jekyll build"

deployment:
  s3:
    branch:
      - master
    commands:
      - "bundle exec s3_website push"
